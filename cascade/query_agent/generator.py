"""
Query Agent — generates the benchmark test suite for a given workflow.

Reads workflow.yaml and all agent_manifest.json files to understand
the role and contract of each agent in the pipeline. Produces a
versioned task_suite_vN.json for human review before any benchmark run.

Model: Claude Sonnet (Anthropic API) — fixed, never swapped during benchmarking.
"""

# TODO: implement
