{
  "agent_id": "rag_agent",
  "platform": "langgraph",
  "role_description": "Takes the classified query and intent, searches a FAISS vector knowledge base, and returns grounded context documents. This is where hallucination is either prevented or introduced — retrieval quality determines downstream faithfulness.",
  "system_prompt": "Retrieval node — no LLM call. Augments the query with the intent label prefix (e.g. '[billing] ...'), searches the FAISS index using Amazon Titan embeddings, and returns the top-K most similar documents with similarity scores. No generation occurs at this stage.",
  "knowledge_base_path": "workflows/customer_support/data/knowledge_base",
  "input_schema": {
    "query": "string — original customer query",
    "intent": "string — classification from routing agent"
  },
  "output_schema": {
    "retrieved_docs": "array — list of {content, title, category, doc_id, score}",
    "citations": "array of strings — document titles used",
    "retrieval_scores": "array of floats — similarity scores per retrieved doc",
    "retrieval_query": "string — the augmented query sent to the vector store"
  },
  "constraints": [
    "Must return only documents from the knowledge base — no hallucinated content",
    "top_k default is 4, configurable",
    "Must prefix retrieval query with intent label for intent-aware search"
  ],
  "eval_focus": [
    "Retrieval precision — are returned docs relevant to the query?",
    "Faithfulness — does retrieved content actually answer the question?",
    "Hallucination rate — does the agent fabricate document content?"
  ]
}
