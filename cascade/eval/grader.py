"""
Grader Agent — scores per-agent outputs for each benchmark run.

Called once per agent output per task. Produces a numeric score (0.0–1.0)
on Task Accuracy and a verdict on Safety Compliance tiers.
Results written to results/{run_id}/grader_output.json for dashboard ingestion.

Model: Claude Sonnet (Anthropic API) — fixed, temperature 0.
Open question: LLM-as-judge vs ground-truth matching (V2 design decision).
"""

# TODO: implement
