[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "cascade-bench"
version = "0.1.0"
description = "Agent-agnostic LLM pipeline benchmarking framework"
requires-python = ">=3.11"
dependencies = []
